fastapi
uvicorn
python-multipart
python-dotenv
openai
pymupdf
pillow
numpy
# --- OCR backends (install the one you use) ---
# default backend: PaddleOCR (local, no API key)
paddleocr
paddlepaddle
# optional alternative local backend:
# easyocr
